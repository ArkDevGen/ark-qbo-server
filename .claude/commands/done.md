Commit all changes and push to GitHub. Follow these steps:

1. Run `git add -A` to stage all changes
2. Look at what changed with `git diff --cached --stat`
3. Write a clear, concise commit message describing what was changed
4. Commit with that message
5. Run `git push` to push to GitHub
6. Confirm to the user that everything is pushed and they're done for the day
